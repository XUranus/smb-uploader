package util

func ExtractAndParseCommandLineArg1(protocol string,cmdArg1 string) error {
	// TODO::

	//base64encoded := cmdArg1[len(protocol):len(cmdArg1) - 1]
	//base64decoded, err := base64.StdEncoding.DecodeString(base64encoded)

	//jsonStr := base64decoded
	//err = json.Unmarshal([]byte(jsonStr), &cmdArgs)

	return nil
}